pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Jenkinsfile successfully checked from GitHub'
            }
        }

        stage('Verify Jenkinsfile') {
            steps {
                echo 'Pipeline is working correctly'
            }
        }

    }
}
